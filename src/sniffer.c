#include <pcap.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "parser.h"

int packetSniffer(char *device) {
    //const int PACKET_BUFFER_SIZE = 65536;  // Maximum packet buffer size in bytes
    //const char error_buffer[PCAP_ERRBUF_SIZE]; // Standard pcap error buffer size

    printf("Dev: %c",*device);
return 0;
}


